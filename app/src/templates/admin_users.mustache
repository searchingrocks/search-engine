<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Management</title>
  <link href="https://cdn.jsdelivr.net/npm/vuetify@2.x/dist/vuetify.min.css" rel="stylesheet">
</head>
<body style="background-color: #121212; color: #f8f9fa;">
  <div id="app">
    <h1>User Management</h1>
    <table border="1" style="width:100%; background-color:#1e1e1e; color:#f8f9fa;">
      <thead>
        <tr>
          <th>ID</th>
          <th>Username</th>
          <th>Role</th>
          <th>Change Password</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        {{#users}}
        <tr>
          <td>{{id}}</td>
          <td>{{username}}</td>
          <td>{{role}}</td>
          <td>
            <form action="/admin/users/{{id}}/changePassword" method="POST">
              <input type="password" name="newPassword" placeholder="New Password" required>
              <button type="submit">Change</button>
            </form>
          </td>
          <td>
            <form action="/admin/users/{{id}}/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this user?');">
              <button type="submit">Delete</button>
            </form>
          </td>
        </tr>
        {{/users}}
      </tbody>
    </table>
  </div>
</body>
</html>
